services:
  postgresql:
    image: postgres:17
    container_name: licit-postgresql
    environment:
      - POSTGRES_USER=licit
      - POSTGRES_PASSWORD=LicitDev2024!
      - POSTGRES_DB=LicitDb
    ports:
      - "5432:5432"
    volumes:
      - postgresql-data:/var/lib/postgresql/data
      - ./init-databases.sql:/docker-entrypoint-initdb.d/init-databases.sql
    networks:
      - licit-network
    healthcheck:
      test: pg_isready -U licit -d LicitDb
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  auth-service:
    build:
      context: ..
      dockerfile: src/Services/AuthService/Dockerfile
    container_name: licit-auth-service
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ConnectionStrings__DefaultConnection=Host=postgresql;Port=5432;Database=LicitAuthDb;Username=licit;Password=LicitDev2024!
      - JwtSettings__Secret=DockerOrtamiIcinGizliAnahtarEnAz32KarakterOlmalidir!
      - JwtSettings__Issuer=Licit.AuthService
      - JwtSettings__Audience=Licit.Client
      - Redis__ConnectionString=redis:6379,password=LicitDev2024!
    ports:
      - "5122:8080"
    depends_on:
      postgresql:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - licit-network

  tendering-service:
    build:
      context: ..
      dockerfile: src/Services/TenderingService/Dockerfile
    container_name: licit-tendering-service
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ConnectionStrings__DefaultConnection=Host=postgresql;Port=5432;Database=LicitTenderingDb;Username=licit;Password=LicitDev2024!
      - JwtSettings__Secret=DockerOrtamiIcinGizliAnahtarEnAz32KarakterOlmalidir!
      - JwtSettings__Issuer=Licit.AuthService
      - JwtSettings__Audience=Licit.Client
      - Redis__ConnectionString=redis:6379,password=LicitDev2024!
      - RabbitMq__Host=rabbitmq
      - RabbitMq__Port=5672
      - RabbitMq__Username=licit
      - RabbitMq__Password=LicitDev2024!
    ports:
      - "5132:8080"
    depends_on:
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - licit-network

  wallet-service:
    build:
      context: ..
      dockerfile: src/Services/WalletService/Dockerfile
    container_name: licit-wallet-service
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ConnectionStrings__DefaultConnection=Host=postgresql;Port=5432;Database=LicitWalletDb;Username=licit;Password=LicitDev2024!
      - JwtSettings__Secret=DockerOrtamiIcinGizliAnahtarEnAz32KarakterOlmalidir!
      - JwtSettings__Issuer=Licit.AuthService
      - JwtSettings__Audience=Licit.Client
    ports:
      - "5142:8080"
    depends_on:
      postgresql:
        condition: service_healthy
    networks:
      - licit-network

  rabbitmq:
    image: rabbitmq:4.2-management
    container_name: licit-rabbitmq
    environment:
      - RABBITMQ_DEFAULT_USER=licit
      - RABBITMQ_DEFAULT_PASS=LicitDev2024!
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    networks:
      - licit-network
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  redis:
    image: redis:8.6-alpine
    container_name: licit-redis
    command: redis-server --requirepass LicitDev2024!
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - licit-network
    healthcheck:
      test: redis-cli -a LicitDev2024! ping | grep PONG
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  mail-service:
    build:
      context: ..
      dockerfile: src/Services/MailService/Dockerfile
    container_name: licit-mail-service
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ConnectionStrings__DefaultConnection=Host=postgresql;Port=5432;Database=LicitMailDb;Username=licit;Password=LicitDev2024!
      - JwtSettings__Secret=DockerOrtamiIcinGizliAnahtarEnAz32KarakterOlmalidir!
      - JwtSettings__Issuer=Licit.AuthService
      - JwtSettings__Audience=Licit.Client
      - SmtpSettings__Host=sandbox.smtp.mailtrap.io
      - SmtpSettings__Port=587
      - SmtpSettings__Username=${MAILTRAP_USERNAME:-username}
      - SmtpSettings__Password=${MAILTRAP_PASSWORD:-password}
      - SmtpSettings__FromEmail=noreply@licit.com
      - SmtpSettings__FromName=Licit
      - RabbitMq__Host=rabbitmq
      - RabbitMq__Port=5672
      - RabbitMq__Username=licit
      - RabbitMq__Password=LicitDev2024!
    ports:
      - "5152:8080"
    depends_on:
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - licit-network

volumes:
  postgresql-data:
  rabbitmq-data:
  redis-data:

networks:
  licit-network:
    driver: bridge
